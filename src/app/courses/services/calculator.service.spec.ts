describe('CalculatorService', () => {
  it('should add two numbers', () => {
    pending(); // for telling jasmine that we are not yet ready for testing this specification.
  });
  it('should subtract two numbers', () => {
    fail(); // we mark it as fail, if we want to write test case that will fail
  })
})
